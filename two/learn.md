## TOPIC
In this example, we are learning hidden form session tracking.

type='hidden' used to hide elements from visible page. But available in source (inspect mode) at user(client-side) end. Browser inspect mode used to see the html page. 
